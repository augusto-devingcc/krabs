export default function Page() {
  return (
    <div className="p-8">
      <p className="font-mono text-sm text-[var(--color-fg-muted)] mb-2"># settings</p>
      <h1 className="text-3xl font-medium mb-2">settings</h1>
      <p className="text-[var(--color-fg-muted)]">
        Coming in a later sub-phase. The contract is live — your agent already
        knows how to work with this entity.
      </p>
    </div>
  );
}
